name: Docker compose dev tag

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

jobs:
  publish:
    uses: ./.github/workflows/docker-publish-workflow.yml
    with:
      tag: dev
    secrets: inherit